NODE_ENV=production

# HTTP server port inside container
PORT=3001

# Allowed origin for REST + Socket.IO CORS
CORS_ORIGIN=https://your-domain.example

# Redis connectivity
REDIS_HOST=redis
REDIS_PORT=6379
